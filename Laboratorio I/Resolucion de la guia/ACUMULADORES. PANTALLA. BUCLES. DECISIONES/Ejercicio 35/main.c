#include <stdio.h>
#include <stdlib.h>

/*35. Hacer un que imprima los números de 0 al 100, controlando las filas y las columnas de forma tal que se
sualicen en pantalla todos a la vez*/

int main()
{
    int i;
    for(i = 0 ; i < 101;i++)
    {
        printf("%d\t",i);
    }
    return 0;
}
